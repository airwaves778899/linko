# Linko MVP dependencies
# Pin to compatible-release ranges so minor updates flow through but breaking
# major bumps don't surprise us.

fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
line-bot-sdk>=3.10,<4.0
litellm>=1.40,<2.0
python-dotenv>=1.0,<2.0
PyYAML>=6.0,<7.0
